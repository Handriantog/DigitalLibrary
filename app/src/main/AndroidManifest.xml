<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.a201381061.digitallearning">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_cloud_circle_black_24dp"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginModule.Activity.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginModule.Activity.LoginActivity" />
        <activity android:name=".LoginModule.Activity.RegisterActivity" />
        <activity android:name=".HomeModule.Activity.MainActivity" />
        <activity android:name=".NewPostModule.Activity.NewPostActivity" />
        <activity android:name=".PostModule.Activity.InsidePostActivity"></activity>
    </application>

</manifest>